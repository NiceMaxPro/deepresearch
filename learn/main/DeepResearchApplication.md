# DeepResearchApplication.java

Spring Boot 应用的主入口类，位于 `com.alibaba.cloud.ai.example.deepresearch` 包下。使用 `@SpringBootApplication` 注解启用自动配置和组件扫描。`main` 方法调用 `SpringApplication.run()` 启动整个 Spring Boot 应用，加载所有配置、控制器、服务、Agent 节点等组件，是深度研究平台的启动点。
