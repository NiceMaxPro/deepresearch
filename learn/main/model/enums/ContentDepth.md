# ContentDepth.java

一个枚举，定义了 `CommunicationPreferences` 使用的三个内容深度级别，用于控制 AI 探索主题的深入程度：`OVERVIEW`（概要，高层次摘要）、`PRACTICAL`（实践，应用/实践深度）和 `CONCEPTUAL`（概念，理论/概念深度）。这使得系统能够根据用户是想要快速概览、可操作的指导还是深入的概念探索来调整研究输出。
