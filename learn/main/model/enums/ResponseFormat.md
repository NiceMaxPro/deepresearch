# ResponseFormat.java

一个枚举，定义了 `CommunicationPreferences` 使用的三种响应格式偏好：`CONCISE`（简洁的）、`DETAILED`（详细的）和 `STRUCTURED_WITH_EXAMPLES`（结构化的，带示例的结构化输出）。这不仅控制 AI 响应的长度，还控制其结构，使系统能够根据用户偏好输出简洁的纯文本、详尽的叙述性文字或包含示例说明的结构化内容。
