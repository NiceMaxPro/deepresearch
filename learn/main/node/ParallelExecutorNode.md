# ParallelExecutorNode

该节点通过将计划步骤分配给可用的执行器实例来编排并行执行。它遍历当前计划中的所有步骤，跳过那些已经有执行结果或状态的步骤。对于 `RESEARCH` 类型的步骤，它立即根据配置的并行节点数量，以轮询方式将它们分配给研究者实例（例如 `researcher_0`、`researcher_1`）。对于 `PROCESSING` 类型（编码）的步骤，它会等待所有研究步骤完成后才将它们分配给编码实例，确保编码阶段只在研究结束后才开始。该节点从 `DeepResearchProperties` 中读取其并行节点数量配置。
