# ProfessionalKbDecisionNode

该节点智能地判断是否需要为当前用户问题查询专业知识库。它首先检查 `RagProperties` 中是否启用了专业知识库决策功能；如果禁用或未配置任何知识库，则立即返回 `use_professional_kb=false`。当启用时，它构建一个决策提示，列出所有可用知识库及其 ID、名称和描述，然后请求 LLM 判断哪些（如果有的话）与用户查询相关。响应从结构化的 `SELECTED: [...]` 格式中解析，如果响应包含肯定指示符，则采用选择第一个知识库的回退策略。选定的知识库 ID 存储在状态中供下游节点使用。
