# FusionStrategy.java

一个接口，定义了将多个已排序的文档结果列表融合为单个重排序列表的契约。它声明了两个方法：`fuse()` 接受一个文档列表的列表（每个列表代表来自一个来源或检索器的结果），返回合并且重排序后的文档列表；`getStrategyName()` 返回策略实现的唯一名称（例如 `"rrf"`）。主要实现是 `RrfFusionStrategy`，它使用倒数排名融合评分。此接口允许不同的融合算法通过公共抽象进行插拔。
