# website-weight-config.json

此 JSON 配置文件定义了特定网站域名的信任权重系数，供搜索和来源评估系统用于对搜索结果的可靠性进行排名。每个条目将一个主机域名映射到一个数值权重，值越接近 1.0 表示可信度越高。当前，`github.com` 被赋予 1.0 的权重（完全可信），`www.baidu.com` 被赋予 0.9（高度可信）。这些权重会输入到研究者代理的来源评估逻辑中，其中高信任权重的搜索结果会被置于结果列表的显著位置，以帮助 AI 模型优先考虑可信来源。
